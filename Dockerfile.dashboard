FROM python:3.11-slim

WORKDIR /app
COPY . .

RUN pip install --no-cache-dir streamlit requests python-dotenv

# Unset Railway's broken variable and run Streamlit
CMD unset STREAMLIT_SERVER_PORT && streamlit run dashboard.py --server.port 8501 --server.address 0.0.0.0
